# ═══════════════════════════════════════════════════════════════════════
# secret-scan allowlist
# ═══════════════════════════════════════════════════════════════════════
# Files listed here (one path per line, exact match) are skipped by
# scripts/secret-scan.sh. Use sparingly — only for files where a literal
# pattern match is unavoidable AND intentional.
#
# Each entry must include a one-line `# why` comment immediately above.
# Lines beginning with `#` and blank lines are ignored.
# ═══════════════════════════════════════════════════════════════════════

# why: documents the rotation procedure; mentions the literal so the operator
#      can recognize what they are rotating away from.
docs/secret_rotation_runbook.md

# why: lists the leak findings; describes the literal in the context of
#      the discovery, not as a usable credential.
docs/platform_safety_findings.md

# why: parameterization design; references the literal in BEFORE/AFTER
#      diff examples so the planned change is understandable.
docs/patches/seed_sql_password_parameterization_plan.md

# why: documents the pattern bank that the scan itself uses; the literal
#      appears in the §4 table.
docs/setup-precommit.md

# why: backlog references the bootstrap-password pattern in item A4 (the
#      scanner backlog item) — discussion only, not a usable credential.
docs/low_effort_improvement_backlog.md

# why: scrub patch shows the literal in `-` context lines (diff format)
#      so reviewers can see exactly what is being removed. The patch
#      itself, once applied, deletes those lines.
docs/patches/DEPLOYMENT_md_scrub.patch
