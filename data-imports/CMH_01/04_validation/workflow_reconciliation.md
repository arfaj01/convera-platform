# Workflow Log Reconciliation

Populated movement-log rows in SMART: 1 (out of 200 SMART scaffolding rows).

Note: this is **expected**. The SMART workbook serves as an authoring template; once claims are imported and transitioned through the platform, the platform itself generates the canonical movement log via /api/claims/transition. The single populated row is the example/sample for documentation purposes.
