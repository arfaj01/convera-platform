# SKIPPED — 010a (legacy) — 010_production_schema.sql

**Status:** intentionally NOT applied to STAGING.

**Reason:** PARALLEL UNIVERSE — v2.0 standalone snapshot using a different access model. Not adopted by 011-035 chain.

**Action required:** none. This file exists only to document why this
migration is missing from the executable section list. Skip past it in
the run order.

**Related execution files:** see `README_EXECUTION.md` for the full
ordered list.
