import { supabase } from '@/lib/supabase';
import type { Profile, UserRole } from '@/lib/types';

export async function getAllUsers() {
  const { data, error } = await supabase.from('­A%,‘QSFAE1BEBQ1